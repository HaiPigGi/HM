const login = require('./controller-login');
const register = require('./controller-register');
const home = require('./controller-home');
const profile = require('./controller-kepengurusan');

module.exports ={
	login,
	register,
	home,
	profile
};